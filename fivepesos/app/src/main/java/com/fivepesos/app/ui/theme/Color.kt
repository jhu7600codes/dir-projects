package com.fivepesos.app.ui.theme

import androidx.compose.ui.graphics.Color

// Matches the flat gray-blue baked into the coin renders and the launcher
// icon, so the UI, the icon, and the coin art all read as one look.
val PesosBackground = Color(0xFF6B7280)
val PesosOnBackground = Color(0xFFFFFFFF)
val PesosCardSurface = Color(0xFFFFFFFF)
val PesosOnCardSurface = Color(0xFF1A1A1A)

// The Settings panel is a deliberate change of register from the rest of
// the app: a full-screen, flat, black "Metro" (Windows Phone) look --
// solid black, one bright accent color, sharp corners, no elevation.
val MetroBackground = Color(0xFF000000)
val MetroAccent = Color(0xFF1BA1E2)
val MetroDivider = Color(0xFF262626)
val MetroSecondaryText = Color(0xFF9A9A9A)
val MetroTertiaryText = Color(0xFF6E6E6E)
val MetroSurface = Color(0xFF141414)
