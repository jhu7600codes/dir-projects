package com.fivepesos.app.ui.theme

import androidx.compose.material3.Typography

// The default type scale reads fine at the sizes this app actually uses
// (button labels and the result line are set explicitly in CoinScreen), so
// there's no need to customize it further here.
val FivePesosTypography = Typography()
