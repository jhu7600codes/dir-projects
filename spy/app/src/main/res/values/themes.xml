<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Actual theming (colors, typography, shapes) lives in Compose
         (ui/theme). This XML theme only controls the window before the
         first frame of Compose content is drawn, so it's kept minimal and
         just matches the app's permanently-dark background to avoid a
         white flash on cold start. -->
    <style name="Theme.Spy" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/window_background</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/window_background</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
</resources>
