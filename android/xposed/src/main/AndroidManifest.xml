<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_share">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_description" />
        <!-- Classic Xposed API version this module was built against. -->
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <!-- LSPosed-specific: scope this module to the Android system
             framework process, since it hooks ResolverActivity/ChooserActivity
             which live there, not in a regular app process. -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

    </application>
</manifest>
