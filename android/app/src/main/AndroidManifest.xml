<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Normal permission; the "draw over other apps" special-access grant it needs
         is requested from Settings on non-rooted devices, or silently granted via
         `appops` on rooted ones — see RootOverlayManager. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".AndrodropApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Androdrop"
        android:supportsRtl="true"
        tools:targetApi="34">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Androdrop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Share target: this is what makes androdrop show up in every app's
             native Android share sheet, no root/Xposed required. -->
        <activity
            android:name=".ui.ShareReceiverActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Androdrop">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- Full-screen accept/decline surface. Launched either via a
             notification full-screen intent (no root needed) or directly by
             the WindowManager overlay's "open full screen" action (root path). -->
        <activity
            android:name=".ui.IncomingTransferActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Androdrop" />

        <service
            android:name=".service.IncomingTransferService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Overlay popup, drawn directly via WindowManager (TYPE_APPLICATION_OVERLAY)
             instead of a full activity, so it can appear instantly on top of
             whatever's on screen without an activity-switch animation. Only used
             once SYSTEM_ALERT_WINDOW is actually granted (see RootOverlayManager). -->
        <service
            android:name=".overlay.OverlayPopupService"
            android:exported="false" />

        <receiver
            android:name=".notify.TransferActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
