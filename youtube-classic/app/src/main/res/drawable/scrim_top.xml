<?xml version="1.0" encoding="utf-8"?>
<!-- angle=90 runs bottom-to-top (startColor at this View's bottom edge,
     endColor at its top edge) - transparent at the bottom, blending into
     the video, dark right at the screen's top edge behind the title/back
     button. See player_control_view.xml for why this exists: white top
     bar text needs *some* scrim behind it on a bright video, same as the
     bottom bar has its own solid black background. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="90"
        android:startColor="#00000000"
        android:endColor="#99000000" />
</shape>
